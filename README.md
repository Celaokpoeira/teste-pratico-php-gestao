# Sistema de Gestão de Compras (Teste Prático PHP)

Este é um sistema web desenvolvido em PHP puro (sem frameworks), MySQL e jQuery para gerenciar o cadastro de Produtos, Fornecedores e seus respectivos vínculos.

## 🛠️ Tecnologias Utilizadas
* PHP 7.4
* MySQL (PDO para comunicação segura)
* HTML5 / CSS3
* jQuery / AJAX (Para comunicação assíncrona)

## ⚙️ Pré-requisitos
* Servidor web local (XAMPP, WAMP, Laragon, etc.) ou ambiente com PHP 7.4+ configurado.
* Servidor MySQL em execução.

## 🚀 Como rodar o projeto localmente

1. **Clone o repositório** para a pasta pública do seu servidor web (ex: `htdocs` no XAMPP ou `www` no WAMP):
   ```bash
   git clone [https://github.com/SEU_USUARIO/NOME_DO_REPOSITORIO.git](https://github.com/SEU_USUARIO/NOME_DO_REPOSITORIO.git)